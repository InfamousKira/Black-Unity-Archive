/* --- Global Styles & Typography --- */
:root {
    --primary-color: #A0522D; /* Sienna (Warm Brown for unity/earth) */
    --secondary-color: #F8F8FF; /* Ghost White (Light text/backgrounds) */
    --background-color: #121212; /* Deep Dark Background */
    --card-background: #1e1e1e; /* Slightly lighter dark for content cards */
    --accent-color: #DAA520; /* Goldenrod (Gold accent for excellence/value) */
    --font-family: 'Georgia', serif;
}

body {
    font-family: var(--font-family);
    background-color: var(--background-color);
    color: var(--secondary-color);
    line-height: 1.6;
    margin: 0;
    padding: 0;
}

h1, h2, h3 {
    color: var(--accent-color);
    font-weight: 700;
}

h1 {
    font-size: 2.5em;
    margin: 0;
}

/* --- Header & Navigation --- */
header {
    background-color: var(--card-background);
    padding: 1em 5%;
    display: flex;
    flex-direction: column;
    align-items: center;
    border-bottom: 3px solid var(--primary-color);
}

nav {
    margin: 1em 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
}

nav button {
    background-color: var(--primary-color);
    color: var(--secondary-color);
    border: none;
    padding: 10px 15px;
    cursor: pointer;
    font-size: 1em;
    border-radius: 5px;
    transition: background-color 0.3s, transform 0.1s;
}

nav button:hover, nav button:focus {
    background-color: var(--accent-color);
    color: var(--background-color);
}

/* --- Search Bar --- */
.search-container {
    width: 100%;
    max-width: 400px;
    margin-top: 10px;
}

#searchInput {
    width: 100%;
    padding: 10px;
    border: 1px solid var(--primary-color);
    border-radius: 5px;
    background-color: var(--background-color);
    color: var(--secondary-color);
    font-size: 1em;
}

/* --- Main Content Layout --- */
main {
    padding: 2em 5%;
}

section {
    display: none; /* All sections are hidden by default */
    padding: 20px 0;
}

section.active {
    display: block; /* Only the active section is shown */
}

/* --- General Content Styling --- */
.content-card {
    background-color: var(--card-background);
    border: 1px solid var(--primary-color);
    border-radius: 8px;
    padding: 20px;
    margin-bottom: 20px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    transition: transform 0.2s;
}

.content-card:hover {
    transform: translateY(-2px);
}

.content-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
}

.card-title {
    color: var(--accent-color);
    cursor: pointer;
}

/* --- Detailed Page --- */
#detailPage {
    padding: 20px;
    background-color: var(--card-background);
    border: 1px solid var(--primary-color);
    border-radius: 8px;
}

#detailPage button {
    background-color: var(--primary-color);
    color: var(--secondary-color);
    border: none;
    padding: 8px 15px;
    margin-bottom: 20px;
    cursor: pointer;
    border-radius: 5px;
}

/* --- Notes Feature (Textareas) --- */
textarea {
    width: 100%;
    min-height: 150px;
    padding: 15px;
    border: 1px solid var(--primary-color);
    border-radius: 5px;
    background-color: var(--background-color);
    color: var(--secondary-color);
    margin-top: 10px;
    box-sizing: border-box; /* Ensures padding is inside the width */
    font-family: var(--font-family);
    font-size: 1em;
}

/* --- Timeline Styling (Simple Vertical) --- */
#timelineContainer {
    position: relative;
    padding: 10px 0;
}

.timeline-event {
    margin: 1em 0;
    border-left: 2px solid var(--accent-color);
    padding-left: 20px;
    position: relative;
}

.timeline-event::before {
    content: '';
    position: absolute;
    left: -7px;
    top: 5px;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background-color: var(--primary-color);
    border: 2px solid var(--accent-color);
}

.timeline-event h4 {
    color: var(--primary-color);
    margin: 0;
}

.timeline-event p {
    font-size: 0.9em;
    margin-top: 0.5em;
}

/* --- Mind Map Container --- */
#mindmapContainer {
    width: 100%;
    height: 600px; /* Needs a fixed height for the Vis.js library to work */
    background-color: var(--background-color);
    border: 1px solid var(--primary-color);
    border-radius: 8px;
    margin-top: 20px;
}

/* --- Links --- */
a {
    color: var(--accent-color);
    text-decoration: none;
    transition: color 0.2s;
}

a:hover {
    color: var(--secondary-color);
    text-decoration: underline;
}

/* --- Utility Class --- */
.hidden {
    display: none !important;
}
